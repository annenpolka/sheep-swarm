{
  "name": "sheep-swarm",
  "version": "0.0.0",
  "private": true,
  "description": "Artifact-driven local coordination: protocol research scaffold",
  "type": "module",
  "license": "UNLICENSED",
  "engines": { "node": ">=24.12.0" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "demo": "node experiments/demo.ts",
    "check:references": "node scripts/check-references.mjs",
    "check": "npm run typecheck && npm test && npm run check:references"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
